{
  "scripts": {
    "install": "(npm install) && (cd server && npm install) && (cd ..) && (cd client && npm install)",
    "init:database": "(cd server && node scripts/create-db.js && node scripts/init-db.js)",
    "drop:database": "(cd server && node scripts/drop-db.js)",
    "test": "(cd server && npm run test) && (cd ..) && (cd client && npm run test:ci)",
    "start:server": "(cd server && npm run dev)",
    "start:client": "(cd client && npm start start)"
  }
}
